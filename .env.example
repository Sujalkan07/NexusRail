# Copy this file to .env and set the runtime database connection string there.
# The application runtime is PostgreSQL-first; SQLite is only for isolated local fallback use.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5433/nexusrail
APP_NAME=NexusRail
